{% extends 'base.html.twig' %}

{% block title %} Taxo Park Home {% endblock %}

{% block body %}

    <h2 class="genus-name">Cars List</h2>

    <table class="table table-striped">
        <thead>
        <tr>
            <th>Car</th>
            <th>Car type</th>
            <th>Work in system</th>
            <th>Change</th>
        </tr>
        </thead>

        <tbody>
        {% for car in cars %}

            <tr>
                <td>
                    <a href="{{ path('car_show',{'carName': car.carName}) }}">{{ car.carName }}</a>
                </td>
                <td>{{ car.CarType }}</td>
                <td>{{ car.updatedAt|date('Y-m-d') }}</td>
                <td><a title="Update CAR info" href="{{ path('car_edit', {'id': car.id}) }}"
                       class="btn btn-xs btn-success"><span class="icon-pencil"></span></a>
                    <a title="Delete CAR" href="{{ path('delete_car', {'id': car.id }) }}"
                       class="btn btn-xs btn-danger"><span class="icon-trash"></span></a>
                </td>
            </tr>

        {% endfor %}

        </tbody>
    </table>




{% endblock %}
